[project]
name = "aegis-observability"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = ["aegis-contracts"]

[tool.uv.sources]
aegis-contracts = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aegis_observability"]

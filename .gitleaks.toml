title = "AEGIS gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Synthetic cross-language contract fixtures and web demo fixtures are not secrets"
paths = [
  '''tests/contract/fixtures/''',
  '''apps/web/fixtures/''',
]

# web

Next.js command-centre web application.

## Ownership

Operator UI shell, graph rendering, and client state (later phases).

## Allowed dependencies

- `@aegis/contracts-ts`, `@aegis/ui`, and approved frontend libraries.
- Must not import `apps/api` or Python packages directly.

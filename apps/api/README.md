# api

FastAPI external HTTP API for AEGIS Command.

## Ownership

Thin route handlers delegating to application services (later phases).

## Allowed dependencies

- `aegis-contracts` and approved Python domain packages.
- Must not import `apps/web` or TypeScript packages.

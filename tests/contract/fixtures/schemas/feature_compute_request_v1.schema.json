{
  "additionalProperties": false,
  "properties": {
    "fromSequence": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Fromsequence"
    },
    "mode": {
      "default": "full",
      "enum": ["incremental", "full"],
      "title": "Mode",
      "type": "string"
    },
    "runId": {
      "title": "Runid",
      "type": "string"
    },
    "schemaVersion": {
      "minimum": 1,
      "title": "Schemaversion",
      "type": "integer"
    },
    "toSequence": {
      "anyOf": [
        {
          "minimum": 0,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Tosequence"
    }
  },
  "required": ["schemaVersion", "runId"],
  "title": "FeatureComputeRequestV1",
  "type": "object"
}

{
  "$defs": {
    "ReportGenerationModeV1": {
      "description": "How the narrative content of a report version was produced.\n\nProvenance, not quality: ``DETERMINISTIC`` reports are assembled purely from persisted\nrun state (events, alerts, evidence, proposals, policy decisions) with no model in the\nloop, and must never be presented to an operator as agent-authored narrative. A run\nwhose agents never produced artifacts \u2014 because the provider was unavailable, or the\nrun was left unattended \u2014 still gets a ``DETERMINISTIC`` report so the after-action\nsurface is populated instead of empty.",
      "enum": ["llm_narrative", "deterministic"],
      "title": "ReportGenerationModeV1",
      "type": "string"
    },
    "ReportGenerationStatusV1": {
      "enum": ["completed", "grounding_fallback", "failed"],
      "title": "ReportGenerationStatusV1",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "checksum": {
      "maxLength": 64,
      "minLength": 64,
      "title": "Checksum",
      "type": "string"
    },
    "createdAt": {
      "title": "Createdat",
      "type": "string"
    },
    "generationMode": {
      "$ref": "#/$defs/ReportGenerationModeV1",
      "default": "deterministic"
    },
    "groundingFallback": {
      "default": false,
      "title": "Groundingfallback",
      "type": "boolean"
    },
    "id": {
      "maxLength": 64,
      "minLength": 1,
      "title": "Id",
      "type": "string"
    },
    "incidentId": {
      "title": "Incidentid",
      "type": "string"
    },
    "promptVersion": {
      "anyOf": [
        {
          "maxLength": 64,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Promptversion"
    },
    "providerId": {
      "anyOf": [
        {
          "maxLength": 64,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Providerid"
    },
    "reportId": {
      "maxLength": 64,
      "minLength": 1,
      "title": "Reportid",
      "type": "string"
    },
    "runId": {
      "title": "Runid",
      "type": "string"
    },
    "schemaVersion": {
      "minimum": 1,
      "title": "Schemaversion",
      "type": "integer"
    },
    "sessionId": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Sessionid"
    },
    "sourceSequenceFrom": {
      "minimum": 0,
      "title": "Sourcesequencefrom",
      "type": "integer"
    },
    "sourceSequenceTo": {
      "minimum": 0,
      "title": "Sourcesequenceto",
      "type": "integer"
    },
    "status": {
      "$ref": "#/$defs/ReportGenerationStatusV1"
    },
    "taskId": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Taskid"
    },
    "versionNumber": {
      "minimum": 1,
      "title": "Versionnumber",
      "type": "integer"
    }
  },
  "required": [
    "schemaVersion",
    "id",
    "runId",
    "incidentId",
    "versionNumber",
    "reportId",
    "status",
    "sourceSequenceFrom",
    "sourceSequenceTo",
    "checksum",
    "createdAt"
  ],
  "title": "ReportVersionV1",
  "type": "object"
}

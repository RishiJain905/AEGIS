{
  "additionalProperties": false,
  "properties": {
    "actorId": {
      "anyOf": [
        {
          "maxLength": 128,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Actorid"
    },
    "expectedRevision": {
      "minimum": 0,
      "title": "Expectedrevision",
      "type": "integer"
    },
    "expectedRevisionId": {
      "maxLength": 64,
      "minLength": 1,
      "title": "Expectedrevisionid",
      "type": "string"
    },
    "idempotencyKey": {
      "maxLength": 256,
      "minLength": 1,
      "title": "Idempotencykey",
      "type": "string"
    },
    "proposalId": {
      "title": "Proposalid",
      "type": "string"
    },
    "reason": {
      "maxLength": 2048,
      "minLength": 1,
      "title": "Reason",
      "type": "string"
    },
    "schemaVersion": {
      "minimum": 1,
      "title": "Schemaversion",
      "type": "integer"
    }
  },
  "required": [
    "schemaVersion",
    "proposalId",
    "expectedRevisionId",
    "expectedRevision",
    "reason",
    "idempotencyKey"
  ],
  "title": "CancelProposalRequestV1",
  "type": "object"
}

{
  "$defs": {
    "ProviderCapability": {
      "enum": ["structured_output", "tools", "vision", "streaming"],
      "title": "ProviderCapability",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "capabilities": {
      "items": {
        "$ref": "#/$defs/ProviderCapability"
      },
      "title": "Capabilities",
      "type": "array"
    },
    "schemaVersion": {
      "minimum": 1,
      "title": "Schemaversion",
      "type": "integer"
    }
  },
  "required": ["schemaVersion"],
  "title": "ProviderCapabilitiesV1",
  "type": "object"
}

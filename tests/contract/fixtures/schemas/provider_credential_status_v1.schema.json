{
  "additionalProperties": false,
  "description": "Whether a user has a usable key for one provider \u2014 never the key itself.",
  "properties": {
    "configured": {
      "title": "Configured",
      "type": "boolean"
    },
    "keyHint": {
      "anyOf": [
        {
          "maxLength": 8,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Keyhint"
    },
    "provider": {
      "maxLength": 64,
      "minLength": 1,
      "title": "Provider",
      "type": "string"
    },
    "schemaVersion": {
      "minimum": 1,
      "title": "Schemaversion",
      "type": "integer"
    },
    "verifiedAt": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Verifiedat"
    }
  },
  "required": ["schemaVersion", "provider", "configured"],
  "title": "ProviderCredentialStatusV1",
  "type": "object"
}

# Phase Handoffs

Each implementation phase creates `NN-phase-slug-HANDOFF.md` in this folder after all required validation commands run.

Handoffs are not progress notes. They are factual compressed context for dependent agents and evidence for the validation agent. Use the exact format in `PHASE-EXECUTION-PROTOCOL.md`.

# models/manifests

Versioned model manifest artifacts for Phase 16+.

## Isolation Forest v1

Path: `isolation-forest-v1/`

Generated by:

```bash
uv run python scripts/train_isolation_forest.py --steps 300
```

Contains:

- `artifact.joblib` — serialized sklearn pipeline with calibration metadata
- `manifest.json` — `ModelManifestV1`
- `training_run.json` — `TrainingRunManifestV1`

Verify:

```bash
uv run python -c "from aegis_ml.models.artifact_store import verify_artifact; print(verify_artifact())"
```
